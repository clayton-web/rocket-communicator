/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package com.aicommunication.assistant.contracts.models

import com.aicommunication.assistant.contracts.models.AssignmentDeliveryStatus
import com.aicommunication.assistant.contracts.models.HandoffDeliveryPath
import com.aicommunication.assistant.contracts.models.Recipient
import com.aicommunication.assistant.contracts.models.Task

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass
import java.io.Serializable

/**
 * Successful Owner handoff result. Does not expose raw capability tokens. Does not imply the human Recipient opened or read the message — only that Gmail accepted send. 
 *
 * @param task Updated Task including assignment attribute after a **successful** handoff (`deliveryStatus=sent`). Version/etag reflect the post-handoff Task concurrency token. 
 * @param deliveryPath 
 * @param deliveryStatus On HTTP 200 success this value is `sent` (Gmail accepted the outbound send). `pending` and `failed` are not returned as successful handoff responses (D092, D042). 
 * @param recipient Owner-visible Recipient summary for the handoff target.
 * @param capabilityId Identifier of the capability issued for this handoff. Raw capability secret is **not** returned on routine handoff — delivery is by email/forward (D063, D094). Administrative raw-token reveal remains `POST /api/v1/tasks/{taskId}/capabilities` only. 
 * @param requiresSendReconsent Always `false` on HTTP 200 success. When handoff fails for missing `gmail.send`, clients should read connection status (`requiresSendReconsent`) and complete OAuth re-consent. 
 * @param idempotentReplay `true` when this response replays a prior successful handoff for the same Idempotency-Key and matching request payload. `false` for a newly completed handoff. 
 */


data class HandoffTaskResponse (

    /* Updated Task including assignment attribute after a **successful** handoff (`deliveryStatus=sent`). Version/etag reflect the post-handoff Task concurrency token.  */
    @Json(name = "task")
    val task: Task,

    @Json(name = "deliveryPath")
    val deliveryPath: HandoffDeliveryPath,

    /* On HTTP 200 success this value is `sent` (Gmail accepted the outbound send). `pending` and `failed` are not returned as successful handoff responses (D092, D042).  */
    @Json(name = "deliveryStatus")
    val deliveryStatus: AssignmentDeliveryStatus,

    /* Owner-visible Recipient summary for the handoff target. */
    @Json(name = "recipient")
    val recipient: Recipient,

    /* Identifier of the capability issued for this handoff. Raw capability secret is **not** returned on routine handoff — delivery is by email/forward (D063, D094). Administrative raw-token reveal remains `POST /api/v1/tasks/{taskId}/capabilities` only.  */
    @Json(name = "capabilityId")
    val capabilityId: kotlin.String,

    /* Always `false` on HTTP 200 success. When handoff fails for missing `gmail.send`, clients should read connection status (`requiresSendReconsent`) and complete OAuth re-consent.  */
    @Json(name = "requiresSendReconsent")
    val requiresSendReconsent: kotlin.Boolean,

    /* `true` when this response replays a prior successful handoff for the same Idempotency-Key and matching request payload. `false` for a newly completed handoff.  */
    @Json(name = "idempotentReplay")
    val idempotentReplay: kotlin.Boolean

) : Serializable {
    companion object {
        private const val serialVersionUID: Long = 123
    }


}

