/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package com.aicommunication.assistant.contracts.models

import com.aicommunication.assistant.contracts.models.TaskSuggestion

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass
import java.io.Serializable

/**
 * Result of an Owner manual capture interpretation (D170).  Deliberately thin. Interpretation provenance is persistence-only and is not published here: no `interpretationRunId`, no InterpretationRun row id, no request fingerprint, no persisted idempotency key, no `modelVersion` or `policyVersion`, and no echo of `rawInput`.  Creates no canonical Task, no approval, no responsibility selection, and no TaskAssignment. Acceptance remains the Owner's through the existing proposal review path. 
 *
 * @param idempotentReplay `true` when this response replays a prior committed interpretation for the same organization, Idempotency-Key, and request payload. A replay is answered from canonical state and does not call the interpretation provider again. 
 * @param interpretedAt When the interpretation that produced these proposals committed. On a replay this is the original occurrence's time, not the time of the replayed request. 
 * @param taskSuggestions The 0..N canonical pending proposals this capture produced, using the same `TaskSuggestion` schema the Owner proposal reads return — there is no second proposal shape. The bound restates the ceiling interpretation output validation already enforces on one call. An empty array is truthful success, not a failure: no placeholder proposal is manufactured. 
 */


data class ManualCaptureResponse (

    /* `true` when this response replays a prior committed interpretation for the same organization, Idempotency-Key, and request payload. A replay is answered from canonical state and does not call the interpretation provider again.  */
    @Json(name = "idempotentReplay")
    val idempotentReplay: kotlin.Boolean,

    /* When the interpretation that produced these proposals committed. On a replay this is the original occurrence's time, not the time of the replayed request.  */
    @Json(name = "interpretedAt")
    val interpretedAt: kotlin.String,

    /* The 0..N canonical pending proposals this capture produced, using the same `TaskSuggestion` schema the Owner proposal reads return — there is no second proposal shape. The bound restates the ceiling interpretation output validation already enforces on one call. An empty array is truthful success, not a failure: no placeholder proposal is manufactured.  */
    @Json(name = "taskSuggestions")
    val taskSuggestions: kotlin.collections.List<TaskSuggestion>

) : Serializable {
    companion object {
        private const val serialVersionUID: Long = 123
    }


}

