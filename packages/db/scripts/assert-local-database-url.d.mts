/**
 * Types for the loopback guard shared by the `:local` Prisma helpers.
 *
 * The implementation is plain `.mjs` so the package scripts and the TypeScript test suite use
 * one copy; this declaration gives the TypeScript side the same checking a `.ts` source would.
 */

export declare function assertLocalDatabaseUrl(rawUrl: unknown): void;
